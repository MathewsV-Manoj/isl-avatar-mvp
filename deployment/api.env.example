# Bind publicly only inside a managed backend service.
ISL_HOST=0.0.0.0
PORT=8080
# Set this to the exact deployed frontend origin; leave empty for local-only use.
ISL_ALLOWED_ORIGIN=https://YOUR-VERCEL-PROJECT.vercel.app
